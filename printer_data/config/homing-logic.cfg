# Note you may need to add the following to your printer.cfg somewhere (without the comments of course) for the Kinematic position stuff below to work.
#[force_move]
#enable_force_move: True

[gcode_macro _HOME_X]
description: Sensorless home X with retry and logging
gcode:
    {% set RUN_CURRENT_X = printer.configfile.settings['tmc2209 stepper_x'].run_current|float %}
    {% set HOME_CURRENT = 0.49 %}
    
    SET_TMC_CURRENT STEPPER=stepper_x CURRENT={HOME_CURRENT}
    {action_respond_info("Homing X: Current set to " ~ HOME_CURRENT)}

    G91
    G1 H2 X5 F300
    G90
    G4 P500

    {% set success = False %}
    {% for attempt in range(1, 4) %}
        {action_respond_info("Homing X: Attempt " ~ attempt)}
        G28 X
        G4 P500

        {% if "x" in printer.toolhead.homed_axes %}
            {action_respond_info("Homing X: Success on attempt " ~ attempt)}
            {% set success = True %}
            {% break %}
        {% else %}
            {action_respond_info("Homing X: Failed on attempt " ~ attempt ~ ", retrying...")}
            G91
            G1 H2 X5 F300
            G90
            G4 P500
        {% endif %}
    {% endfor %}

    {% if not success %}
        {action_respond_error("Homing X failed after 3 attempts")}
    {% endif %}

    SET_TMC_CURRENT STEPPER=stepper_x CURRENT={RUN_CURRENT_X}
    {action_respond_info("Homing X complete. Restored current to " ~ RUN_CURRENT_X)}


[gcode_macro _HOME_Y]
description: Sensorless home Y with retry and logging
gcode:
    {% set RUN_CURRENT_Y = printer.configfile.settings['tmc2209 stepper_y'].run_current|float %}
    {% set HOME_CURRENT = 0.49 %}

    SET_TMC_CURRENT STEPPER=stepper_y CURRENT={HOME_CURRENT}
    {action_respond_info("Homing Y: Current set to " ~ HOME_CURRENT)}

    G91
    G1 H2 Y5 F300
    G90
    G4 P500

    {% set success = False %}
    {% for attempt in range(1, 4) %}
        {action_respond_info("Homing Y: Attempt " ~ attempt)}
        G28 Y
        G4 P500

        {% if "y" in printer.toolhead.homed_axes %}
            {action_respond_info("Homing Y: Success on attempt " ~ attempt)}
            {% set success = True %}
            {% break %}
        {% else %}
            {action_respond_info("Homing Y: Failed on attempt " ~ attempt ~ ", retrying...")}
            G91
            G1 H2 Y5 F300
            G90
            G4 P500
        {% endif %}
    {% endfor %}

    {% if not success %}
        {action_respond_error("Homing Y failed after 3 attempts")}
    {% endif %}

    SET_TMC_CURRENT STEPPER=stepper_y CURRENT={RUN_CURRENT_Y}
    {action_respond_info("Homing Y complete. Restored current to " ~ RUN_CURRENT_Y)}


[homing_override]
axes: xyz
gcode:
  {% set home_all = 'X' not in params and 'Y' not in params and 'Z' not in params %}

  # Raise Z to clear nozzle
  SET_KINEMATIC_POSITION Z=1
  G91
  G1 Z10 F1500
  G90

  {% if home_all or 'X' in params %}
    {action_respond_info("Begin homing X")}
    _HOME_X
  {% endif %}

  {% if home_all or 'Y' in params %}
    {action_respond_info("Begin homing Y")}
    _HOME_Y
  {% endif %}

  {% if home_all or 'Z' in params %}
    {action_respond_info("Moving to center for Z homing")}
    G90
    G1 X150 Y150 F9000

    {% set success = False %}
    {% for attempt in range(1, 4) %}
        {action_respond_info("Homing Z: Attempt " ~ attempt)}
        G28 Z
        G4 P500

        {% if "z" in printer.toolhead.homed_axes %}
            {action_respond_info("Homing Z: Success on attempt " ~ attempt)}
            {% set success = True %}
            {% break %}
        {% else %}
            {action_respond_info("Homing Z: Failed on attempt " ~ attempt ~ ", retrying...")}
            G91
            G1 Z3 F300
            G90
            G4 P500
        {% endif %}
    {% endfor %}

    {% if not success %}
        {action_respond_error("Homing Z failed after 3 attempts")}
    {% endif %}
  {% endif %}

  {action_respond_info("Homing complete")}



[gcode_macro _CG28]
description: Conditional homing if axes not already homed
gcode:
    {% if "x" not in printer.toolhead.homed_axes or 
           "y" not in printer.toolhead.homed_axes or 
           "z" not in printer.toolhead.homed_axes %}
        G28
    {% else %}
        {action_respond_info("Skipping homing — all axes already homed")}
    {% endif %}
# Note you may need to add the following to your printer.cfg somewhere (without the comments of course) for the Kinematic position stuff below to work.
#[force_move]
#enable_force_move: True

[gcode_macro _HOME_X]
description: Sensorless home X toward max with retries and logging
gcode:
    {% set RUN_CURRENT_X = printer.configfile.settings['tmc2209 stepper_x'].run_current|float %}
    {% set HOME_CURRENT = 0.59 %}
    
    SET_TMC_CURRENT STEPPER=stepper_x CURRENT={HOME_CURRENT}
    {action_respond_info("Homing X: Set current to " ~ HOME_CURRENT)}

    G91
    G1 H2 X-5 F300  ; nudge away from max
    G90
    G4 P500

    G91
    G1 H2 X320 F120  ; move toward MAX end
    G90
    G4 P500
    QUERY_ENDSTOPS

    {% if "x" in printer.toolhead.homed_axes %}
        SET_KINEMATIC_POSITION X=300
        {action_respond_info("Homing X: Success, position set to X=300")}
    {% else %}
        G91
        G1 H2 X-5 F300
        G90
        G4 P500
        G91
        G1 H2 X320 F120
        G90
        G4 P500
        QUERY_ENDSTOPS
        {% if "x" in printer.toolhead.homed_axes %}
            SET_KINEMATIC_POSITION X=300
            {action_respond_info("Homing X: Retry success, position set to X=300")}
        {% else %}
            {action_respond_error("Homing X failed after 2 attempts")}
        {% endif %}
    {% endif %}

    SET_TMC_CURRENT STEPPER=stepper_x CURRENT={RUN_CURRENT_X}





[gcode_macro _HOME_Y]
description: Sensorless home Y toward max with retries and logging
gcode:
    {% set RUN_CURRENT_Y = printer.configfile.settings['tmc2209 stepper_y'].run_current|float %}
    {% set HOME_CURRENT = 0.59 %}
    
    SET_TMC_CURRENT STEPPER=stepper_y CURRENT={HOME_CURRENT}
    {action_respond_info("Homing Y: Set current to " ~ HOME_CURRENT)}

    G91
    G1 H2 Y-5 F300  ; nudge away from max
    G90
    G4 P500

    G91
    G1 H2 Y320 F120  ; move toward MAX end
    G90
    G4 P500
    QUERY_ENDSTOPS

    {% if "y" in printer.toolhead.homed_axes %}
        SET_KINEMATIC_POSITION Y=305
        {action_respond_info("Homing Y: Success, position set to Y=305")}
    {% else %}
        G91
        G1 H2 Y-5 F300
        G90
        G4 P500
        G91
        G1 H2 Y320 F120
        G90
        G4 P500
        QUERY_ENDSTOPS
        {% if "y" in printer.toolhead.homed_axes %}
            SET_KINEMATIC_POSITION Y=305
            {action_respond_info("Homing Y: Retry success, position set to Y=305")}
        {% else %}
            {action_respond_error("Homing Y failed after 2 attempts")}
        {% endif %}
    {% endif %}

    SET_TMC_CURRENT STEPPER=stepper_y CURRENT={RUN_CURRENT_Y}


[homing_override]
axes: xyz
gcode:
  {% set home_all = 'X' not in params and 'Y' not in params and 'Z' not in params %}

  # Raise Z to prevent scraping
  G91
  G1 Z10 F1500
  G90

  {% if home_all or 'X' in params %}
    _HOME_X
  {% endif %}

  {% if home_all or 'Y' in params %}
    _HOME_Y
  {% endif %}

  {% if home_all or 'Z' in params %}
    {action_respond_info("Z Homing: Moving to center")}
    G90
    G1 X150 Y150 F9000
    G28 Z
  {% endif %}

  {action_respond_info("Homing complete")}



[gcode_macro _CG28]
description: Conditional homing if axes not already homed
gcode:
    {% if "x" not in printer.toolhead.homed_axes or 
           "y" not in printer.toolhead.homed_axes or 
           "z" not in printer.toolhead.homed_axes %}
        G28
    {% else %}
        {action_respond_info("Skipping homing — all axes already homed")}
    {% endif %}
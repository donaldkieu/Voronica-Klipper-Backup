# Note you may need to add the following to your printer.cfg somewhere (without the comments of course) for the Kinematic position stuff below to work.
#[force_move]
#enable_force_move: True

[gcode_macro _HOME_X]
description: Sensorless home X with retry and forced movement
gcode:
    {% set RUN_CURRENT_X = printer.configfile.settings['tmc2209 stepper_x'].run_current|float %}
    {% set HOME_CURRENT = 0.6 %}

    SET_TMC_CURRENT STEPPER=stepper_x CURRENT={HOME_CURRENT}
    {action_respond_info("Homing X: Set current to " ~ HOME_CURRENT)}

    # Force re-homing by invalidating current position
    SET_KINEMATIC_POSITION X=9999

    G91
    G1 H2 X5 F300
    G90
    G4 P500

    G28 X
    G4 P500
    {% if "x" in printer.toolhead.homed_axes %}
        {action_respond_info("Homing X: Success on attempt 1")}
    {% else %}
        G91
        G1 H2 X5 F300
        G90
        G4 P500
        G28 X
        G4 P500
        {% if "x" in printer.toolhead.homed_axes %}
            {action_respond_info("Homing X: Success on attempt 2")}
        {% else %}
            G91
            G1 H2 X5 F300
            G90
            G4 P500
            G28 X
            G4 P500
            {% if "x" in printer.toolhead.homed_axes %}
                {action_respond_info("Homing X: Success on attempt 3")}
            {% else %}
                {action_respond_error("Homing X failed after 3 attempts")}
            {% endif %}
        {% endif %}
    {% endif %}

    SET_TMC_CURRENT STEPPER=stepper_x CURRENT={RUN_CURRENT_X}
    {action_respond_info("Homing X complete. Current restored to " ~ RUN_CURRENT_X)}



[gcode_macro _HOME_Y]
description: Sensorless home Y with retry and forced movement
gcode:
    {% set RUN_CURRENT_Y = printer.configfile.settings['tmc2209 stepper_y'].run_current|float %}
    {% set HOME_CURRENT = 0.6 %}

    SET_TMC_CURRENT STEPPER=stepper_y CURRENT={HOME_CURRENT}
    {action_respond_info("Homing Y: Set current to " ~ HOME_CURRENT)}

    SET_KINEMATIC_POSITION Y=9999

    G91
    G1 H2 Y5 F300
    G90
    G4 P500

    G28 Y
    G4 P500
    {% if "y" in printer.toolhead.homed_axes %}
        {action_respond_info("Homing Y: Success on attempt 1")}
    {% else %}
        G91
        G1 H2 Y5 F300
        G90
        G4 P500
        G28 Y
        G4 P500
        {% if "y" in printer.toolhead.homed_axes %}
            {action_respond_info("Homing Y: Success on attempt 2")}
        {% else %}
            G91
            G1 H2 Y5 F300
            G90
            G4 P500
            G28 Y
            G4 P500
            {% if "y" in printer.toolhead.homed_axes %}
                {action_respond_info("Homing Y: Success on attempt 3")}
            {% else %}
                {action_respond_error("Homing Y failed after 3 attempts")}
            {% endif %}
        {% endif %}
    {% endif %}

    SET_TMC_CURRENT STEPPER=stepper_y CURRENT={RUN_CURRENT_Y}
    {action_respond_info("Homing Y complete. Current restored to " ~ RUN_CURRENT_Y)}


[homing_override]
axes: xyz
gcode:
  {% set home_all = 'X' not in params and 'Y' not in params and 'Z' not in params %}

  # Raise Z to avoid scraping
  SET_KINEMATIC_POSITION Z=1
  G91
  G1 Z10 F1500
  G90

  {% if home_all or 'X' in params %}
    _HOME_X
  {% endif %}

  {% if home_all or 'Y' in params %}
    _HOME_Y
  {% endif %}

  {% if home_all or 'Z' in params %}
    {action_respond_info("Z Homing: Moving to center")}
    G90
    G1 X150 Y150 F9000

    G28 Z
    G4 P500
    {% if "z" in printer.toolhead.homed_axes %}
        {action_respond_info("Z Homing: Success on attempt 1")}
    {% else %}
        G91
        G1 Z3 F300
        G90
        G4 P500
        G28 Z
        G4 P500
        {% if "z" in printer.toolhead.homed_axes %}
            {action_respond_info("Z Homing: Success on attempt 2")}
        {% else %}
            G91
            G1 Z3 F300
            G90
            G4 P500
            G28 Z
            G4 P500
            {% if "z" in printer.toolhead.homed_axes %}
                {action_respond_info("Z Homing: Success on attempt 3")}
            {% else %}
                {action_respond_error("Z Homing failed after 3 attempts")}
            {% endif %}
        {% endif %}
    {% endif %}
  {% endif %}

  {action_respond_info("Homing complete")}



[gcode_macro _CG28]
description: Conditional homing if axes not already homed
gcode:
    {% if "x" not in printer.toolhead.homed_axes or 
           "y" not in printer.toolhead.homed_axes or 
           "z" not in printer.toolhead.homed_axes %}
        G28
    {% else %}
        {action_respond_info("Skipping homing — all axes already homed")}
    {% endif %}